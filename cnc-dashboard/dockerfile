FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
# Note the change: 'dashboard:server' tells Gunicorn to look in dashboard.py
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "dashboard:server"]